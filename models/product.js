class Product {
  constructor(
    name,
    price,
    screen,
    backCameram,
    frontCamera,
    desc,
    img,
    type,
    quantity
  ) {
    (this.name = name),
      (this.price = price),
      (this.screen = screen),
      (this.backCameram = backCameram),
      (this.frontCamera = frontCamera),
      (this.desc = desc),
      (this.img = img),
      (this.type = type),
      (this.quantity = quantity);
  }
}
